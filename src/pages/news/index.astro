---
import Layout from '../../layouts/Layout.astro'; import PageHero from '../../components/PageHero.astro'; import { news } from '../../data/site';
---
<Layout title="お知らせ"><PageHero title="お知らせ" lead="つなぐ屋からのお知らせです。" eyebrow="NEWS" /><section class="mx-auto max-w-4xl px-5 py-16"><div class="divide-y divide-stone-200 border-y border-stone-200">{news.map((item) => <article class="flex flex-col gap-3 py-6 sm:flex-row sm:items-center"><time class="w-28 text-sm text-stone-400">{item.date}</time><span class="w-fit rounded bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-800">{item.category}</span><h2 class="font-bold text-emerald-950">{item.title}</h2></article>)}</div></section></Layout>
