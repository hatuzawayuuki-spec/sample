---
import type { Case } from '../data/site';
import { link } from '../lib/paths';
const { item } = Astro.props as { item: Case };
---
<article class="overflow-hidden rounded-2xl border border-stone-200 bg-white shadow-sm"><div class="grid h-44 place-items-center bg-gradient-to-br from-amber-50 to-stone-100 text-6xl" aria-hidden="true">{item.image}</div><div class="p-5"><div class="flex items-center justify-between text-xs"><span class="rounded-full bg-emerald-50 px-3 py-1 font-bold text-emerald-800">{item.item}</span><time class="text-stone-400">{item.date}</time></div><h3 class="mt-4 font-bold leading-6 text-emerald-950">{item.title}</h3><p class="mt-3 text-sm text-stone-500">{item.brand} / {item.store}</p><p class="mt-2 text-lg font-bold text-emerald-800">参考価格 ¥{item.price.toLocaleString()}</p><a href={link(`/cases/${item.slug}/`)} class="mt-4 inline-flex text-sm font-bold text-emerald-800">詳細を見る →</a></div></article>
